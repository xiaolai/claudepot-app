Decoy: parseInt's lenient prefix parsing would read `2026` as pid.
The strict filename regex `/^\d+\.json$/` must reject this file so
it is not swept on a stale scan. See concurrentSessions.ts:186
(anthropics/claude-code#34210 — silent user data loss bug).
