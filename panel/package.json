{
  "name": "claudepot-panel",
  "private": true,
  "version": "0.0.0",
  "license": "ISC",
  "type": "module",
  "description": "The remote panel served by `claudepot remote serve`. Its own install on purpose — see README.md.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:render": "node scripts/render-check.mjs",
    "check:render:self-test": "node scripts/render-check.mjs --self-test",
    "test": "node --test \"src/**/*.test.js\""
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "mermaid": "^11.16.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
    "vite": "^7.3.6",
    "jsdom": "^29.0.2"
  }
}
