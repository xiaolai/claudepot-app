{
  "grid": {
    "min-card": {
      "$value": "280px",
      "$type": "dimension",
      "$description": "Minimum card width in auto-fill grids (e.g., /projects). Drives column count: at &lt;560px → 1 col, &lt;840px → 2 col, &lt;1120px → 3 col, &gt;= 1120px → 4 col."
    }
  },
  "sp": {
    "36": {
      "$value": "2.25rem",
      "$type": "dimension",
      "$description": "36px — fills the gap between sp.32 and sp.40 for the nav logo size"
    }
  }
}
